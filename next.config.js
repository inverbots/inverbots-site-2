/**zz @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ['nidoapartamentos.co', 'movemoshost.com', 'tierralinda.movemospruebas.com', 'admin.tierralindacampestre.com.co']
	}
}

module.exports = nextConfig
